import { useCallback, useEffect, useMemo, useState } from 'react'

import { Check, Globe2, LoaderCircle } from '../../icons'
import { toUserMessage } from '../../shared/api/errors'
import { ErrorBanner } from '../../shared/ui/Feedback'
import { ConfirmationDialog } from '../organizations/components/ConfirmationDialog'
import { complianceApi } from './api/complianceApi'
import { MetaConnectorPanel } from './MetaConnectorPanel'


const SOURCE_KINDS = [['csv', 'CSV'], ['facebook', 'Facebook'], ['linkedin', 'LinkedIn'], ['open_data', 'Données ouvertes'], ['api', 'API'], ['other', 'Autre']]
const PURPOSES = [['commercial_follow_up', 'Suivi commercial'], ['customer_relationship', 'Relation client'], ['supplier_relationship', 'Relation fournisseur']]
const CATEGORIES = [['business_identity', 'Identité de l’établissement'], ['person_identity', 'Identité de personne'], ['email', 'Courriel'], ['phone', 'Téléphone'], ['social_profile', 'Profil social']]
const PROVIDER_STATUS = { draft: 'Brouillon — non utilisable', active: 'Actif', suspended: 'Suspendu — non utilisable', retired: 'Retiré — non utilisable' }
const ACQUISITION_STATUS = { pending_review: 'En attente de revue', approved: 'Approuvée', quarantined: 'En quarantaine', rejected: 'Rejetée' }

function isoDate(value) { return value ? String(value).slice(0, 10) : '' }
export function ProvidersAcquisitionsPage({ session }) {
  const locale = session.active_organization?.locale === 'en-CA' ? 'en-CA' : 'fr-CA'
  const sourceKinds = locale === 'en-CA' ? [['csv', 'CSV'], ['facebook', 'Facebook'], ['linkedin', 'LinkedIn'], ['open_data', 'Open data'], ['api', 'API'], ['other', 'Other']] : SOURCE_KINDS
  const purposes = locale === 'en-CA' ? [['commercial_follow_up', 'Sales follow-up'], ['customer_relationship', 'Customer relationship'], ['supplier_relationship', 'Supplier relationship']] : PURPOSES
  const categories = locale === 'en-CA' ? [['business_identity', 'Business identity'], ['person_identity', 'Person identity'], ['email', 'Email'], ['phone', 'Phone'], ['social_profile', 'Social profile']] : CATEGORIES
  const copy = locale === 'en-CA' ? { error: 'Unable to load compliance data.', heading: 'Source compliance', title: 'Providers and acquisitions', description: 'Document the origin before creating CRM data. An approved acquisition never grants permission to contact.', providers: 'Providers', acquisitions: 'Acquisitions', connectors: 'Connectors', loading: 'Loading…', newProvider: 'New provider', sourceType: 'Source type', providerName: 'Provider name', createDraft: 'Create draft', registered: 'Registered providers', validUntil: 'Valid until', terms: 'Terms', edit: 'Edit', none: 'No provider declared.', status: 'Status', contractRef: 'Contract reference', termsUrl: 'Terms URL', validFrom: 'Valid from', territories: 'Territories (comma-separated)', purposes: 'Allowed purposes', categories: 'Allowed categories', rights: 'I attest that I have the necessary rights for this source.', close: 'Close', save: 'Save', declare: 'Declare an acquisition', provider: 'Provider', type: 'Type', sourceLabel: 'Source label', territory: 'Territory', purpose: 'Purpose', obtained: 'Date obtained', external: 'External reference (optional)', declaredCategories: 'Declared categories', declareButton: 'Declare acquisition', compatibleProvider: 'Activate and attest to a compatible provider before declaring an acquisition.', history: 'Acquisition history', historyHelp: 'Origin approval does not create permission to contact.', reason: 'Reason', approve: 'Approve', reject: 'Reject', noAcquisition: 'No acquisition declared.', approveTitle: 'Approve this acquisition?', rejectTitle: 'Reject this acquisition?', approveText: 'The origin will become usable without automatically authorizing contact.', rejectText: 'The declaration will remain audited but cannot be used as an origin.' } : { error: 'Impossible de charger la conformité.', heading: 'Conformité des sources', title: 'Fournisseurs et acquisitions', description: 'Documentez l’origine avant toute création de données CRM. Une acquisition approuvée ne vaut jamais permission de contact.', providers: 'Fournisseurs', acquisitions: 'Acquisitions', connectors: 'Connexions', loading: 'Chargement…', newProvider: 'Nouveau fournisseur', sourceType: 'Type de source', providerName: 'Nom du fournisseur', createDraft: 'Créer le brouillon', registered: 'Fournisseurs enregistrés', validUntil: 'Valide jusqu’au', terms: 'Conditions', edit: 'Modifier', none: 'Aucun fournisseur déclaré.', status: 'État', contractRef: 'Référence contractuelle', termsUrl: 'URL des conditions', validFrom: 'Valide à partir du', territories: 'Territoires (séparés par virgules)', purposes: 'Finalités autorisées', categories: 'Catégories autorisées', rights: 'J’atteste disposer des droits nécessaires pour cette source.', close: 'Fermer', save: 'Enregistrer', declare: 'Déclarer une acquisition', provider: 'Fournisseur', type: 'Type', sourceLabel: 'Libellé de la source', territory: 'Territoire', purpose: 'Finalité', obtained: 'Date d’obtention', external: 'Référence externe (facultatif)', declaredCategories: 'Catégories déclarées', declareButton: 'Déclarer l’acquisition', compatibleProvider: 'Activez et attestez un fournisseur compatible avant de déclarer une acquisition.', history: 'Historique des acquisitions', historyHelp: 'L’approbation de provenance ne crée aucune permission de contact.', reason: 'Motif', approve: 'Approuver', reject: 'Rejeter', noAcquisition: 'Aucune acquisition déclarée.', approveTitle: 'Approuver cette acquisition ?', rejectTitle: 'Rejeter cette acquisition ?', approveText: 'La provenance deviendra utilisable sans autoriser automatiquement le contact.', rejectText: 'La déclaration restera auditée mais ne pourra pas servir de provenance.' }
  const [tab, setTab] = useState('providers')
  const [providers, setProviders] = useState([])
  const [acquisitions, setAcquisitions] = useState([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  const [success, setSuccess] = useState('')
  const [selectedProvider, setSelectedProvider] = useState(null)
  const [decision, setDecision] = useState(null)
  const canManageProviders = session.capabilities.includes('providers:manage')
  const canDeclare = session.capabilities.includes('acquisitions:declare')
  const canReview = session.capabilities.includes('acquisitions:review')

  const load = useCallback(async () => {
    setLoading(true); setError('')
    try {
      const [providerPage, acquisitionPage] = await Promise.all([complianceApi.listProviders(), complianceApi.listAcquisitions()])
      setProviders(providerPage.items ?? []); setAcquisitions(acquisitionPage.items ?? [])
    } catch (requestError) { setError(toUserMessage(requestError, copy.error)) } finally { setLoading(false) }
  }, [copy.error])
  useEffect(() => { load() }, [load])
  const activeProviders = useMemo(() => providers.filter((provider) => provider.status === 'active'), [providers])

  async function createProvider(event) {
    event.preventDefault(); const formElement = event.currentTarget; const form = new FormData(formElement)
    try { const created = await complianceApi.createProvider({ source_kind: form.get('source_kind'), label: String(form.get('label')).trim() }); setProviders((items) => [created, ...items]); formElement.reset(); setSuccess('Fournisseur créé en brouillon. Complétez son attestation avant toute utilisation.') } catch (requestError) { setError(toUserMessage(requestError, 'Impossible de créer le fournisseur.')) }
  }
  async function saveProvider(event) {
    event.preventDefault(); if (!selectedProvider) return; const form = new FormData(event.currentTarget)
    const values = (name) => form.getAll(name).map(String)
    const territories = String(form.get('territories')).split(',').map((item) => item.trim()).filter(Boolean)
    const purposes = values('purpose')
    const categories = values('category')
    try {
      const updated = await complianceApi.updateProvider(selectedProvider.id, {
        version: selectedProvider.version, label: String(form.get('label')).trim(), status: form.get('status'), terms_reference: String(form.get('terms_reference')).trim() || undefined, terms_url: String(form.get('terms_url')).trim() || undefined, valid_from: form.get('valid_from') ? new Date(`${form.get('valid_from')}T00:00:00Z`).toISOString() : undefined, valid_until: form.get('valid_until') ? new Date(`${form.get('valid_until')}T23:59:59Z`).toISOString() : undefined,
        ...(territories.length ? { allowed_territories: territories } : {}),
        ...(purposes.length ? { allowed_purposes: purposes } : {}),
        ...(categories.length ? { allowed_data_categories: categories } : {}),
        rights_attested: form.get('rights_attested') === 'on',
      })
      setProviders((items) => items.map((item) => item.id === updated.id ? updated : item)); setSelectedProvider(updated); setSuccess('Fournisseur enregistré.')
    } catch (requestError) { setError(toUserMessage(requestError, 'Impossible de modifier le fournisseur.')) }
  }
  async function declareAcquisition(event) {
    event.preventDefault(); const formElement = event.currentTarget; const form = new FormData(formElement); const categories = form.getAll('category').map(String)
    try { const created = await complianceApi.declareAcquisition({ source_kind: form.get('source_kind'), source_label: String(form.get('source_label')).trim(), provider_id: form.get('provider_id'), purpose: form.get('purpose'), territory: String(form.get('territory')).trim(), obtained_at: new Date(`${form.get('obtained_at')}T12:00:00Z`).toISOString(), data_categories: categories, external_reference: String(form.get('external_reference')).trim() || undefined }); setAcquisitions((items) => [created, ...items]); formElement.reset(); setSuccess(created.status === 'approved' ? 'Acquisition déclarée et approuvée selon les règles du fournisseur.' : 'Acquisition déclarée ; elle requiert une revue ou reste en quarantaine.') } catch (requestError) { setError(toUserMessage(requestError, 'Impossible de déclarer l’acquisition.')) }
  }
  async function confirmDecision() {
    if (!decision) return
    try { const updated = await complianceApi.decideAcquisition(decision.item.id, { version: decision.item.version, decision: decision.action, reason_code: decision.action === 'reject' ? 'manual_review_rejected' : undefined }); setAcquisitions((items) => items.map((item) => item.id === updated.id ? updated : item)); setSuccess(decision.action === 'approve' ? 'Acquisition approuvée. Cette approbation ne vaut pas permission de contact.' : 'Acquisition rejetée.'); setDecision(null) } catch (requestError) { setError(toUserMessage(requestError, 'Impossible d’enregistrer la décision.')); setDecision(null) }
  }

  return <main className="administration-page compliance-page" aria-labelledby="compliance-title">
    <header className="administration-page-heading"><p className="eyebrow">{copy.heading}</p><h1 id="compliance-title">{copy.title}</h1><p>{copy.description}</p></header>
    {error && <ErrorBanner><span>{error}</span></ErrorBanner>}{success && <div className="success-banner" role="status"><Check size={18} /><span>{success}</span></div>}
    <div className="administration-tabs" role="tablist" aria-label={copy.title}><button type="button" role="tab" aria-selected={tab === 'providers'} onClick={() => setTab('providers')}>{copy.providers}</button><button type="button" role="tab" aria-selected={tab === 'acquisitions'} onClick={() => setTab('acquisitions')}>{copy.acquisitions}</button><button type="button" role="tab" aria-selected={tab === 'connectors'} onClick={() => setTab('connectors')}>{copy.connectors}</button></div>
    {loading ? <div className="administration-loading" aria-live="polite"><LoaderCircle className="spin" size={20} /> {copy.loading}</div> : tab === 'providers' ? <>
      {canManageProviders && <section className="administration-card compliance-form-card"><h2>{copy.newProvider}</h2><form className="compliance-form compact-form" onSubmit={createProvider}><label>{copy.sourceType}<select name="source_kind" defaultValue="csv">{sourceKinds.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>{copy.providerName}<input name="label" maxLength="160" required /></label><button className="primary-button" type="submit">{copy.createDraft}</button></form></section>}
      <section className="administration-card"><h2>{copy.registered}</h2><ul className="compliance-list">{providers.map((provider) => <li key={provider.id}><div><strong>{provider.label}</strong><small>{sourceKinds.find(([value]) => value === provider.source_kind)?.[1] ?? provider.source_kind}</small><span className={`resource-status ${provider.status}`}>{PROVIDER_STATUS[provider.status] ?? provider.status}</span></div><div>{provider.valid_until && <small>{copy.validUntil} {isoDate(provider.valid_until)}</small>}{provider.terms_url && <a href={provider.terms_url} target="_blank" rel="noopener noreferrer"><Globe2 size={14} /> {copy.terms}</a>}</div>{canManageProviders && <button className="secondary-button" type="button" onClick={() => setSelectedProvider(provider)}>{copy.edit}</button>}</li>)}</ul>{!providers.length && <p className="form-help">{copy.none}</p>}</section>
      {selectedProvider && <section className="administration-card compliance-form-card"><h2>{copy.edit} {selectedProvider.label}</h2><form className="compliance-form" onSubmit={saveProvider}><label>{copy.providerName}<input name="label" defaultValue={selectedProvider.label} required /></label><label>{copy.status}<select name="status" defaultValue={selectedProvider.status}>{Object.entries(PROVIDER_STATUS).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>{copy.contractRef}<input name="terms_reference" defaultValue={selectedProvider.terms_reference ?? ''} /></label><label>{copy.termsUrl}<input name="terms_url" type="url" defaultValue={selectedProvider.terms_url ?? ''} /></label><label>{copy.validFrom}<input name="valid_from" type="date" defaultValue={isoDate(selectedProvider.valid_from)} /></label><label>{copy.validUntil}<input name="valid_until" type="date" defaultValue={isoDate(selectedProvider.valid_until)} /></label><label className="full">{copy.territories}<input name="territories" defaultValue={(selectedProvider.allowed_territories ?? []).join(', ')} placeholder="CA-QC, CA-ON" /></label><fieldset className="full"><legend>{copy.purposes}</legend>{purposes.map(([value,label]) => <label key={value}><input type="checkbox" name="purpose" value={value} defaultChecked={selectedProvider.allowed_purposes?.includes(value)} /> {label}</label>)}</fieldset><fieldset className="full"><legend>{copy.categories}</legend>{categories.map(([value,label]) => <label key={value}><input type="checkbox" name="category" value={value} defaultChecked={selectedProvider.allowed_data_categories?.includes(value)} /> {label}</label>)}</fieldset><label className="full"><input name="rights_attested" type="checkbox" defaultChecked={Boolean(selectedProvider.rights_attested_at)} /> {copy.rights}</label><div className="form-actions"><button className="secondary-button" type="button" onClick={() => setSelectedProvider(null)}>{copy.close}</button><button className="primary-button" type="submit">{copy.save}</button></div></form></section>}
    </> : tab === 'connectors' ? <MetaConnectorPanel session={session} providers={providers} acquisitions={acquisitions} /> : <>
      {canDeclare && <section className="administration-card compliance-form-card"><h2>{copy.declare}</h2>{activeProviders.length ? <form className="compliance-form" onSubmit={declareAcquisition}><label>{copy.provider}<select name="provider_id" required>{activeProviders.map((provider) => <option key={provider.id} value={provider.id}>{provider.label}</option>)}</select></label><label>{copy.type}<select name="source_kind" defaultValue={activeProviders[0].source_kind}>{SOURCE_KINDS.map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>{copy.sourceLabel}<input name="source_label" required maxLength="160" /></label><label>{copy.territory}<input name="territory" defaultValue="CA-QC" required maxLength="16" /></label><label>{copy.purpose}<select name="purpose" defaultValue="commercial_follow_up">{PURPOSES.map(([value,label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>{copy.obtained}<input name="obtained_at" type="date" required /></label><label className="full">{copy.external}<input name="external_reference" maxLength="128" /></label><fieldset className="full"><legend>{copy.declaredCategories}</legend>{CATEGORIES.map(([value,label], index) => <label key={value}><input type="checkbox" name="category" value={value} defaultChecked={index === 0} /> {label}</label>)}</fieldset><button className="primary-button" type="submit">{copy.declareButton}</button></form> : <p className="form-help">{copy.compatibleProvider}</p>}</section>}
      <section className="administration-card"><h2>{copy.history}</h2><p className="form-help">{copy.historyHelp}</p><ul className="compliance-list">{acquisitions.map((item) => <li key={item.id}><div><strong>{item.source_label}</strong><small>{item.territory} · {isoDate(item.obtained_at)}</small><span className={`resource-status ${item.status}`}>{ACQUISITION_STATUS[item.status] ?? item.status}</span></div><div><small>{item.data_categories.join(', ')}</small>{item.decision_reason_code && <small>{copy.reason}: {item.decision_reason_code}</small>}</div>{canReview && ['pending_review', 'quarantined'].includes(item.status) && <div className="resource-actions"><button className="secondary-button" type="button" onClick={() => setDecision({ item, action: 'approve' })}>{copy.approve}</button><button className="danger-link" type="button" onClick={() => setDecision({ item, action: 'reject' })}>{copy.reject}</button></div>}</li>)}</ul>{!acquisitions.length && <p className="form-help">{copy.noAcquisition}</p>}</section>
    </>}
    {decision && <ConfirmationDialog title={decision.action === 'approve' ? copy.approveTitle : copy.rejectTitle} confirmLabel={decision.action === 'approve' ? copy.approve : copy.reject} onCancel={() => setDecision(null)} onConfirm={confirmDecision}><p>{decision.action === 'approve' ? copy.approveText : copy.rejectText}</p></ConfirmationDialog>}
  </main>
}
